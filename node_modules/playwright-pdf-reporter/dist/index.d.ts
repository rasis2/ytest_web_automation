import { FullConfig, FullResult, Reporter, ReporterOptions, Suite, TestCase, TestResult } from './types';
export default class PlaywrightPdfReporter implements Reporter {
    private readonly options;
    private config?;
    private suite?;
    private readonly cases;
    private readonly caseMap;
    private readonly warnings;
    constructor(options?: ReporterOptions);
    onBegin(config: FullConfig, suite: Suite): void;
    onTestEnd(test: TestCase, result: TestResult): Promise<void>;
    onEnd(result: FullResult): Promise<void>;
    private caseKey;
    private buildCaseId;
    private initializeCaseDetail;
    private mergeAttempt;
    private toAttemptDetail;
    private collectAttachments;
}
export { PlaywrightPdfReporter };
export type { ReporterOptions } from './types';
//# sourceMappingURL=index.d.ts.map