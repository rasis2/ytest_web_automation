"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.writeHistory = exports.readHistory = void 0;
const path_1 = __importDefault(require("path"));
const fs_1 = require("./utils/fs");
const readHistory = async (historyPath) => {
    if (!historyPath) {
        return [];
    }
    const absolute = path_1.default.resolve(historyPath);
    if (!(await (0, fs_1.fileExists)(absolute))) {
        return [];
    }
    const raw = await (0, fs_1.readFileSafe)(absolute);
    if (!raw) {
        return [];
    }
    try {
        const parsed = JSON.parse(raw);
        return Array.isArray(parsed) ? parsed : [];
    }
    catch {
        return [];
    }
};
exports.readHistory = readHistory;
const writeHistory = async (historyPath, entries) => {
    if (!historyPath)
        return;
    const absolute = path_1.default.resolve(historyPath);
    await (0, fs_1.ensureDir)(path_1.default.dirname(absolute));
    await (0, fs_1.writeFileSafe)(absolute, JSON.stringify(entries, null, 2));
};
exports.writeHistory = writeHistory;
//# sourceMappingURL=historyManager.js.map